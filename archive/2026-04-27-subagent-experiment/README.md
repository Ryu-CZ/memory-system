# Removed Subagent Experiment Notes

This archive marker records that early subagent workflow experiment notes were removed from the active sandbox documentation surface.

Those notes helped bootstrap the sandbox, but they became static context and could distract future `memory-inbox` development.

Current active memory work starts at:

- `../../memory/README.md`
- `../../memory/memory-inbox/README.md`
- `../../memory/memory-inbox/docs/product-decision.md`

This directory is not a source of requirements.
